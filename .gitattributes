# .pen documents are encrypted binary. With core.autocrlf=true (this
# workstation) Git would otherwise treat them as text and rewrite their
# line endings on checkout, corrupting the file.
*.pen binary
